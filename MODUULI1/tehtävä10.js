let logmessages = '';

const noppa = +prompt('Syötä noppien määrä: ');
const summa = +prompt('Syötä haluttu silmälukujen summa: ');

for (let i = 1; i <= noppa; i++) {
    if ( ){

    }
    logmessages += `Noppa ${i}; Silmäluku: ${silmaluku}.`
    console.log(`Noppa ${i}; Silmäluku: ${silmaluku}.`);
}

document